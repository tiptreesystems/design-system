# Graduated component CSS

No component CSS is currently published. A `css/components/<name>.css` source
enters this directory only after a production consumer satisfies
`docs/ADDING_A_COMPONENT.md`.

This tracked file also keeps the optional `css/` authoring directory present in
fresh checkouts; the development watcher remains defensive if it is absent.
